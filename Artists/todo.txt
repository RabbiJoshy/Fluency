== Guide for Claude ==
This is Josh's backlog. Items here are NOT instructions to start working — they are ideas and plans.
- If there is a "Suggested next session" block below, use it as context for the conversation — but verify the current state of things first (files may have changed since it was written). Remove it once it's been acted on.
- Do NOT start any item without Josh explicitly asking in the current conversation.
- DO mention relevant items when they come up naturally ("this relates to item X on the todo, want to tackle it?").
- Priority labels: [high] = next up, [low] = someday, [deferred] = don't even suggest unless Josh brings it up.
- Done items can be cleaned out periodically when Josh says so.
- See `todo_details.md` for expanded context on each item (for Claude, not for Josh to read).
- Do NOT use preview mode when working on items from this list. Service worker caching makes previews unreliable — Josh tests in his own browser.
====================

== Suggested next session ==
Level estimation was rebuilt as adaptive staircase (one word at a time, translation reveal, retest option). Josh wants to test it more. Potential follow-up: use general Spanish vocabulary for estimation even in artist mode (less genre-biased). Also: normal mode parity is the next big feature piece.
====================


TO DO
-----

- [high] Normal mode parity — artist mode is much further ahead. Normal mode is missing example sentence cycling, easiness-based sorting, and the improved card layout. Big piece.
- Use general Spanish vocabulary for level estimation even in artist mode (less genre-biased). Needs mapping strategy from general rank to artist deck.
- Cross-artist MWE detection — currently step 3 detects MWEs per-artist only. A shared detection pass across all artist corpora would find expressions that don't cross the frequency threshold in any single artist. Master vocab already unions MWE memberships, but detection itself is per-artist.
- [low] Sense dedup/mapping — master vocab treats "to do" and "to do, to make" as distinct senses (exact string match). A future pass could merge near-duplicate senses (e.g., "am" vs "I am", minor translation variants).
- [low] Spotify links: Bad Bunny done (248/302 matched). Rosalía lookup still needed — run `python3 Artists/scripts/spotify_lookup.py` after Spotify rate limit resets. ~54 Bad Bunny songs missed due to Genius/Spotify title mismatches (could manually map later).
- Research more public sources for translated artist lyrics beyond Genius. Would reduce Gemini costs when adding new artists and improve translation coverage. Worth a thorough look at what's available.
- [low] Surface per-word "known lyrics %" somewhere in settings — show what percentage of lyrics the user can understand based on words they've marked as known.
- [deferred] Auto-populate albums dictionaries from Genius album pages. Currently manually curated per artist.


VOCABULARY ISSUES
-----------------
Items here are things Josh noticed while using the app. When fixing, don't just fix the individual word — investigate whether it's a symptom of a bigger pipeline/data problem. Delete items from this list once resolved.

- fixed rinde — translated as "sniper" but the word "rinde" doesn't appear in the example text where it says sniper. Possible Genius translation line misalignment (known issue with section-aware zipping). Investigate why "sniper" was matched to this word.
- fixed bellacoso — shows frequency 20 but only has one example sentence. How does a word appear 20 times but only generate one example?
- calle has 2 translatins, and they are both correct, but only one has example lyrics.

SONGS TO EXCLUDE
----------------
Songs Josh noticed that shouldn't be in the corpus (remixes, live versions, non-artist songs, etc.). When fixing, add to duplicate_songs.json and check if there are similar songs that should also be excluded. Delete items from this list once resolved.

(none currently)


DONE
----

- Level estimation rebuilt as adaptive staircase (one word at a time, translation reveal, retest, convergence via streak + small step). Replaced batch binary search.
- Multi-artist image selection uses per-example artist slug for correct album art fallback.
- Multi-artist merge recalculates meaning frequency from example counts instead of stale Gemini line assignments.
- Fixed English TTS voice selection (Samantha over Daniel, blocklisted novelty voices). Service worker switched to network-first.
- Decided against per-artist verse filtering. Existing filters handle the real problems. Genius verse labels are unreliable.
- Bracket/adlib stripping in step 3. Easiness scoring strips adlibs and ignores interjections/English/proper nouns.
- Improved button touch targets.
- Example lyrics show artist name alongside song name.
