# wrangler's local state: miniflare's SQLite copy of the D1 data, the cached
# Cloudflare account identity, and build temp files. Local-only, and the D1
# copy contains real user progress.
.wrangler/

# Generated by seed.py from a Sheets dump; contains user progress.
seed*.sql
